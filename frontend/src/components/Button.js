
const Button = ({ text }) => {
    return (
      <button type="submit" name="send" className="bg-slate-700 p-2 rounded-xl text-base text-slate-300 font-semibold">{text}</button>
    )
  }
  
  export default Button